version: v1.0
name: Unit testing
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
blocks:
  - name: Unit tests
    task:
      prologue:
        commands:
          - 'export PATH=$HOME/.local/bin:$PATH'
          - checkout
          - mkdir .pip_cache
          - cache restore
          - >-
            python3.7 -m pip install --user --cache-dir .pip_cache -r
            requirements.txt
          - cache store
          - python3 --version
          - python3.7 --version
          - cd tests
      jobs:
        - name: Configuration
          commands:
            - python3.7 -m unittest test_configuration -v
        - name: Compilation
          commands:
            - python3.7 -m unittest test_compilation -v
      epilogue:
        always:
          commands:
            - ls -la
    dependencies:
      - Python version
  - name: Python version
    dependencies: []
    task:
      jobs:
        - name: Set up Python
          commands:
            - sem-version python 3.7
